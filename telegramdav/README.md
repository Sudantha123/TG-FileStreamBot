# TelegramDAV

Telegram-backed WebDAV gateway written in Go.

Endpoints: `/dav`, `/web`, `/healthz`.

Configuration is supplied through environment variables. GitHub Actions builds a Linux amd64 binary artifact.
